# Placeholder variables for local development
# Backend variables are consumed by server/*
DATABASE_URL=postgresql://user:password@localhost:5432/tester
JWT_SECRET=replace-with-a-strong-secret-min-16-chars
JWT_EXPIRES_IN=1h
JWT_ISSUER=tester.com
JWT_AUDIENCE=tester.com-web

# Optional frontend variable (if needed by app)
VITE_API_BASE_URL=http://127.0.0.1:3001/api
