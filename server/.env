# JWT settings
JWT_SECRET=troque-por-um-segredo-forte
JWT_EXPIRES_IN=1h
JWT_ISSUER=tester.com
JWT_AUDIENCE=tester.com-web

# Bcrypt pepper — appended to every password before hashing/comparing
# Must match in all routes: register, login, update-password
BCRYPT_PEPPER=Reinaldo2026
